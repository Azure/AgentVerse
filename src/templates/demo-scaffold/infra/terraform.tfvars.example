# Copy to terraform.tfvars and fill in. Do NOT commit terraform.tfvars (it's gitignored).

resource_group       = "rg-{{demo-name}}-demo"
location             = "eastus2"
foundry_account_name = "{{demoname}}foundry"   # globally-unique, lowercase, <= 24 chars
model_deployment_name = "gpt-4.1"
