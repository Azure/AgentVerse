"""Agents for the production-scheduling demo.

Agent folders use kebab-case (repo convention), so import them with importlib:

    import importlib
    orchestrator = importlib.import_module("agents.schedule-orchestrator.agent")
"""
