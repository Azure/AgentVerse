# Terraform working files
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Local variable files (may contain environment-specific, non-secret config)
terraform.tfvars
demos.auto.tfvars
*.auto.tfvars
!*.auto.tfvars.example

# Keep the provider lock file committed (.terraform.lock.hcl)
